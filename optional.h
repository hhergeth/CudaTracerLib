#pragma once

// optional is still experimental in visual c++, remove this later on and replace by <optional> include
#include <optional.hpp>
namespace std {
using namespace experimental;
}  // namespace std;